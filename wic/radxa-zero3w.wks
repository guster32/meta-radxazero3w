# short-description: Create SD card image with a boot partition
# long-description:
# Create an image that can be written onto a SD card using dd for use
# with Radxa Zero3w style boards .
# It uses SPL, and u-boot
#
# The disk layout used is:
#  -------------------------------------------
# | Area Name | From (sector #)| To (Sector #)|
# |-------------------------------------------|
# | Bootloader |  64	           | 20543        |
# | BOOT	  |  20544	       | 151615       |
# | rootfs	  |  151616	       | -            |
#  -------------------------------------------
#

# Bootloader with GPT partition table
bootloader --ptable gpt

# Complete bootloader (SPL + U-Boot FIT image) - starts at sector 64 (32KB offset)
part --source rawcopy --sourceparams="file=u-boot-rockchip.bin" --offset 32 --fixed-size 10M --no-table

# BOOT Partition - starts at sector 20544 (10MB offset)
part --source bootimg-partition --ondisk mmcblk --fstype=ext2 --label boot --active --offset 10272 --fixed-size 64M

# rootfs Partition
part --source rootfs --ondisk mmcblk --fstype=ext4 --label rootfs --align 2048
